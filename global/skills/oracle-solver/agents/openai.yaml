interface:
  display_name: "Oracle Solver"
  short_description: "Independent judgment for difficult decisions"
  default_prompt: "Use $oracle-solver for a bounded independent judgment under the current evidence and constraints, then return the managed review path before continuing."
