Objective:
Project/worktree and plan evidence paths:
Verified state:
Remaining work:
Scope and explicit permissions:
Implementation judgment and orchestration:

Use the global handoff guidance and omit fields that do not apply. For cross-project work, include the exact contract, source version, sample output, and destination task; distinguish the sent request from the returned result.
